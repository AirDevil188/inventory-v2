<html>
  <head>
    <title><%= title %></title>
    <body>
      <%- include('navbar') %>
      <main>
        <% if (genre) { %>
            <section class="detail-section">
                <div class="title-container">
                    <h3><%= genre.genre_name %></h3>
                </div>
               <% if (games) { %>
                <div class="items-container">
                    <ul>
                     <% games.forEach(game => { %>
                       <li><a href="<%= game.game_url %>"><%= game.game_title %></a></li>
                    <% }) %>
                   </ul>
                </div>
               <% } %>

               <div class="button-contianer">
                <button type="submit"><a href="<%=genre.url %>/delete">DELETE</a></button>
                <button type="submit"><a href="<%=genre.url %>/update">UPDATE</a></button>
            </section>
              <%  } else { %>
                <div class="error-container">
                    <span>Genre was not found!</span>
                </div>
                <% }%>
            
      </main>
    </body>
  </head>
</html>
