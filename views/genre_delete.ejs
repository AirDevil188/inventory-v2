<html>
    <head>
        <title><%= title %> <%= genre.name %> </title>
        <body>
            <main>
                <section class="delete-section">
                   <% if (genre) { %> 
                    <section class="delete-section">
                        <% if (games.length) { %>
                            <span>Please make sure to first delete the games that are associated with this genre!</span>
                            <url>
                                <h4>List of Games:</h4>
                                <% games.forEach(game => { %>
                                    <li><a href="<%= game.game_url%>"><%= game.game_title%></a></li>
                                <% }) %>
                            </url>
                            <div class="button-container">
                                <button type="button"><a href="<%=genre.url %>">Cancel</a></button>
                            </div>
                        <% }  else { %>
                            <h3><%= title +  " " + genre.genre_name %> genre </h3>
                            <form action="<%=genre.url %>/delete/" method="post">
                                <button type="submit">YES</button>
                                <button><a href="<%=genre.url %>">Cancel</a></button>
                            </form>

                        <% }%>
                    </section>
                    <%  } else { %>
                   <% } %>
                </section>
            </main>
        </body>

    </head>
</html>